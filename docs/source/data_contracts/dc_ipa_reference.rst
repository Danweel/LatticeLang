.. _dc_ipa_reference:

IPA Reference Data Contract
===========================

:data structure: IPAReference
:source: derived from PHOIBLE 2.0 (:cite:p:`moran2019`) and the
   IPA chart (:cite:p:`ipa1999`)
:generated by: ``scripts/derive_ipa_reference.py`` (Q38)
:consumed by: UC-01 (inventory pre-fill), UC-07 (export /
   rendering), UC-012 (segmentation lookup), dc_phoneme (known
   vs. custom determination)
:status: Draft — schema complete; build script not yet written
  (Q38); this contract is its input specification

.. note::
   This contract was reviewed during the 2026-09-16 use-case
   and spec-completeness audit.

Overview
--------

``ipa_reference.json`` is the **read-only reference table** of
valid IPA symbols shipped with LatticeLang. It is Floor 1 of the
architecture: reference data, not user data. The program never
modifies it at runtime; it is regenerated only by the derive
script during development.

It answers three questions for any string the user might type:

1. Is this a valid IPA symbol? (membership)
2. What is it like? (features, description, place/manner)
3. How sonorous is it? (proposed sonority rank)

The user's phoneme inventory (:ref:`dc_phoneme`) is a *subset*
of this table, plus custom symbols not in it. Membership in the
reference table determines the ``custom`` flag on phonemes.

Location
--------

The reference table is stored as a single JSON file in the data
directory: ``data/ipa_reference.json``. Consonant/vowel subsets
are runtime filters, not separate files — a few hundred entries
load in milliseconds, and multiple files would multiply the
update surface to no benefit.

Top-Level Structure
--------------------

.. literalinclude:: ../../../tests/fixtures/ipa_reference.example.json
   :language: json
   :caption: Example ``ipa_reference.json``

Full entry examples are contained in the included fixture;
one entry per array demonstrates each schema below.

IPA Reference Data Fields
-------------------------

.. list-table::
   :header-rows: 1
   :widths: 25 75

   * - Field
     - Description
   * - ``symbol``
     - Canonical IPA form (tie bar for affricates: ``t͡s``); unique across the whole table
   * - ``description``
     - Human-readable description, for tooltips and help text
   * - ``place`` / ``manner`` / ``voicing``
     - Consonant classification fields
   * - ``height`` / ``backness`` / ``roundedness``
     - Vowel classification fields
   * - ``features``
     - Major-class feature bundle; keys from the pinned PHOIBLE 2.0 vocabulary (:ref:`ADR-033`)
   * - ``sonority_rank``
     - Integer 0–9, or explicit null (UC-01 treats null as a prompt)
   * - ``unicode_points``
     - Array of ``U+XXXX`` strings; concatenates to the symbol
   * - ``canonical_forms``
     - Alternate representations (e.g., ``"ts"`` for ``"t͡s"``); includes the ``symbol`` itself
   * - ``aliases``
     - Non-canonical typed forms (e.g., ``"ch"``, ``"tsh"``); normalize to ``symbol`` on hit
   * - ``tipa``
     - TIPA macro string, or null where none exists; consumed by UC-07's exporter
   * - ``phoible_frequency``
     - Decimal 0–1; fraction of PHOIBLE 2.0 inventories containing the segment (computed by the derive script, Q38)
   * - ``rarity_tier``
     - Integer 1–5 (universal → rare); tier 6 (unattested) pending :ref:`q36-rarity-tier-finalization`
   * - ``combination_type``
     - ``special_combinations`` only: ``tie_bar | length | syllabic_mark`` parsing-rule discriminator
   * - ``constituents``
     - ``special_combinations`` only: ordered array of the base symbols / combining marks the entry is built from

Top-level fields: ``schema_version``, ``pinned_sources`` (release
pins), and ``attribution`` (PHOIBLE 2.0, CC-BY 4.0 — per
:ref:`ADR-029`, attribution lives in the JSON metadata, not only
the bibliography).

Entry Schema — Consonants
-------------------------

Each ``consonants`` entry carries the common field set from the
table above plus three classification fields:

``place``
   Place of articulation, following the IPA chart's row labels
   (e.g., ``bilabial``, ``alveolar``, ``post_alveolar``,
   ``velar``). Exact spellings are fixed by the derive script's
   vocabulary at build time; consumers compare against the
   emitted values, not the chart's typography.

``manner``
   Manner of articulation (e.g., ``plosive``, ``fricative``,
   ``affricate``, ``nasal``, ``approximant``).

``voicing``
   ``"voiceless"`` or ``"voiced"``.

All other fields are as in the field summary above. The ``p``
entry in the included fixture is a complete example; its
``symbol`` is already in normalized tie-bar form (:ref:`ADR-028`),
trivially so for a single-codepoint segment.

Entry Schema — Vowels
---------------------

Each ``vowels`` entry uses the same common field set, with three
vowel classification fields in place of ``place``, ``manner``,
and ``voicing``:

``height``
   Vowel height: ``close``, ``near-close``, ``close-mid``,
   ``mid``, ``open-mid``, ``near-open``, ``open``.

``backness``
   ``front``, ``central``, or ``back``.

``roundedness``
   ``rounded`` or ``unrounded``.

``manner``/``voicing`` do not apply. Every other field —
``features`` through ``tipa``, plus ``phoible_frequency`` and
``rarity_tier`` — is identical in semantics to the consonant
schema. The ``ɑ`` entry in the included fixture is a complete
example.

Special Combinations
--------------------

Symbols outside the single-chart entries, handled explicitly:

- **Tie-bar compositions** (``t͡s``, ``a͡ɪ``) — legal segment
  notation (ADR-028); represented with combining characters.
  Not subject to diacritic near-miss comparison (:ref:`ADR-041`).
- **Length and gemination marks** (ː) — carried for parsing;
  duration semantics are the ``duration`` concept in the glossary.
- **Syllabicity marks** (n̩) — parsed to ``syllabic: "+"``
  feature overrides.

Entries in ``special_combinations`` use the full schema of their
parent class (consonant or vowel) plus two fields:

``combination_type``
   Discriminator naming the entry's parsing rule:
   ``tie_bar``, ``length``, or ``syllabic_mark``. It says *how*
   the symbol parses, not *which* symbol it is — many entries
   share a type (``t͡s`` and ``k͡p`` are both ``tie_bar``).

``constituents``
   Ordered array of the strings the entry is built from. For
   ``tie_bar`` and ``length``: the base phoneme symbols
   (``["t", "s"]``). For ``syllabic_mark``: the base phoneme
   plus the combining mark (``["n", "̩"]``). Constituents
   identify the inventory-level phonemes a combination derives
   from — raw codepoints encode characters but say nothing
   about inventory membership, which UC-012 needs for
   un-tie-barred input.

Field Checks
------------

Validated by the derive script at build time (and by a load-time
sanity check):

#. Every ``symbol`` is unique across the whole table.
#. Every consonant and vowel entry has a ``sonority_rank``
   value or an explicit null (null is handled; absence is not).
#. Every ``unicode_points`` array is non-empty and the codepoints
   concatenate to the symbol.
#. Every alias resolves to its entry's ``symbol``.
#. Every ``features`` key is in the pinned PHOIBLE 2.0 vocabulary.
#. Sonority rank ties at boundary ranks are permitted (glide
   and close vowel both may rank 8 — expected, not anomalous;
   see :ref:`dc_phoneme`'s rank table note).
#. Every ``special_combinations`` entry carries a
   ``combination_type`` from ``tie_bar | length |
   syllabic_mark`` and a ``constituents`` array of at least
   two non-empty strings.
#. Every ``canonical_forms`` array includes the entry's
   ``symbol``.
#. Every entry has a ``rarity_tier`` in 1–5 and a
   ``phoible_frequency`` in [0, 1] (or explicit null, tier 6
   only). Threshold bucketing feeds :ref:`q36-rarity-tier-finalization`.

Build Pipeline (Q38)
--------------------

``scripts/derive_ipa_reference.py`` regenerates the table:

1. Parse the pinned PHOIBLE 2.0 release for symbol–feature
   pairs and attestation frequencies.
2. Filter to the IPA chart inventory (:cite:p:`ipa1999`).
3. Derive ``sonority_rank`` per the derivation table (basis
   :cite:p:`clements1990`; :cite:p:`kramer2020` simplification
   for nasals), emitting null where the derivation is
   uncertain rather than guessing.
4. Merge a small curated overrides file (hand-maintained,
   ``data/overrides.json``): descriptions, aliases, tipa
   macros, and any feature corrections. Curated wins over derived.
5. Run the field checks above; fail the build on any violation.

The script is developed test-first; this contract's Field
Checks are its acceptance tests. Test-first sketch
(``tests/test_ipa_reference.py``):

.. code-block:: python

   def test_loads_and_has_fields():
       """Every entry: symbol, features, sonority_rank or
       explicit null, tipa or explicit null."""
       ref = load_ipa_reference()
       assert len(ref.consonants) + len(ref.vowels) > 200

   def test_symbols_unique():
       """Field check 1: no duplicate symbols anywhere."""

   def test_aliases_resolve():
       """Field check 4: every alias maps to its symbol."""

   def test_tie_bar_not_diacritic_near_miss():
       """ADR-041 exclusion: t͡s vs ts must not compare as
       near-misses."""

Relations
---------

- Superset of: the user's phoneme inventory (:ref:`dc_phoneme` membership test for the ``custom`` flag)
- Feeds: UC-01 (pre-fill features, rank, frequency), UC-07 (tipa for export), UC-012 (segmentation lookup, aliases)
- Governed by: :ref:`ADR-033` (pinned PHOIBLE 2.0 feature vocabulary), :ref:`ADR-028` (tie-bar normalization), :ref:`ADR-041` (near-miss exclusions)
- Is NOT: a phoneme inventory (it has no user data), nor a preset (presets are :ref:`dc_language_definition` instances)

References
----------

- :cite:p:`ipa1999` — the IPA Handbook; chart source.
- :cite:p:`moran2019` — PHOIBLE 2.0; feature and attestation data feeding the derive script.
- :cite:p:`clements1990` — sonority hierarchy basis for rank derivation.
- :cite:p:`kramer2020` — the dual nasal hypothesis: nasals are not a single sonority class (grounds the rank table's 4–5 range for nasals rather than a fixed rank).