.. _troubleshooting_latex_compilation_fails:
.. _latex_compliation_fails:

Troubleshooting: LaTeX Compilation Fails
========================================

:Status: Stub — content pending (referenced by UC-07, extension 4a)

.. todo::
   :class: warning

   **Write this page.** Triggered by UC-07 extension 4a (compilation
   fails on the user's machine). Expected content: common causes
   (``tipa`` not installed, encoding issues, missing ``utf8`` input
   encoding), the minimal-example ``.tex`` snippet for isolating the
   problem, and when the failure is the user's environment vs. an
   export bug.